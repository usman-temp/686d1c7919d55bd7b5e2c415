'use client';import { motion, useScroll } from 'framer-motion';import Image from 'next/image';export default function Home() {  const { scrollYProgress } = useScroll();  const recipes = [    { id: 1, title: 'Italian Pasta', time: '30 mins', image: '/next.svg' },    { id: 2, title: 'French Desserts', time: '45 mins', image: '/vercel.svg' },  ];  return (    <main className="min-h-screen">      <motion.div        className="fixed top-0 left-0 right-0 h-1 bg-orange-600 origin-left"        style={{ scaleX: scrollYProgress }}        aria-label="Scroll progress indicator"      />      {/* Hero Section */}      <section className="px-6 py-20 md:py-32 text-center">        <motion.h1          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ duration: 0.8 }}          className="text-4xl md:text-6xl font-bold text-orange-600 mb-6"          role="heading"          aria-level="1"        >          Master the Art of Cooking        </motion.h1>        <motion.p          initial={{ opacity: 0 }}          animate={{ opacity: 1 }}          transition={{ delay: 0.4 }}          className="text-lg md:text-xl text-gray-600 mb-8 max-w-2xl mx-auto"          aria-label="Website description"        >          Discover recipes, techniques, and culinary inspiration from around the world.        </motion.p>      </section>      {/* Featured Recipes */}      <section aria-labelledby="featured-recipes-heading" className="px-6 py-16">        <h2 id="featured-recipes-heading" className="text-3xl font-bold text-center mb-12 text-gray-800">          Featured Recipes        </h2>        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">          {recipes.map((recipe, index) => (            <motion.article              key={recipe.id}              initial={{ opacity: 0, scale: 0.9 }}              whileInView={{ opacity: 1, scale: 1 }}              transition={{ delay: index * 0.2 }}              viewport={{ once: true, margin: "-100px" }}              className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow"              role="article"              aria-labelledby={`recipe-${recipe.id}-title`}            >              <div className="relative h-48 w-full">                <Image                  src={recipe.image}                  alt={recipe.title}                  fill                  className="object-cover"                  sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"                />              </div>              <div className="p-6">                <h3 id={`recipe-${recipe.id}-title" className="text-xl font-semibold mb-2">                  {recipe.title}                </h3>                <p className="text-gray-500">{recipe.time}</p>              </div>            </motion.article>          ))}        </div>      </section>      {/* Why Us Section Link */}      <motion.div        className="text-center py-16"        initial={{ opacity: 0 }}        animate={{ opacity: 1 }}        transition={{ delay: 1.2 }}        role="region"        aria-label="Why Choose Us section"      >        <a          href="/why-us"          className="inline-block bg-orange-600 text-white px-8 py-3 rounded-lg hover:bg-orange-700 transition-colors text-lg"          aria-label="Learn why to choose us"        >          Why Choose Us?        </a>      </motion.div>    </main>  );}